# ~/.config/mako/config

# --- Basic Setup ---
font=JetBrainsMono Nerd Font 10
# default-timeout=5000 # Disappear after 5 seconds (already discussed)
layer=overlay
anchor=top-right

# --- Appearance ---
# Semi-transparent background
# Example: A dark background (like #2E3440 from your Waybar) with ~70% opacity (AA in hex)
# To calculate Alpha (AA): opacity_percentage * 255 / 100.
# For 70% opacity: 0.70 * 255 = 178.5. In hex, 178 is B2.
# For ~60% opacity: 0.60 * 255 = 153. In hex, 153 is 99.
# For ~80% opacity: 0.80 * 255 = 204. In hex, 204 is CC.
# Let's use something like a dark grey with decent transparency. #333333B3 (B3 is ~70% opacity)

# Dark background (Nord Polar Night) with ~80% opacity
background-color=#2E3440CC

# Light text (Nord Snow Storm)
text-color=#D8DEE9

# Rounded corners
# Adjust for more or less rounding
border-radius=10

# Border (optional, but helps define the rounded shape)
border-size=2
# A slightly lighter border, also semi-transparent (Nord Polar Night)
border-color=#4C566AEE

# Padding inside the notification
padding=10

# Margin around notifications (if you have multiple stacked)
margin=5

# Width and Height (optional, mako can size automatically)
# width=300
# max-height=100

# --- Other useful options (optional) ---
# icons=1                     # Show icons if available
# max-visible=3               # Max number of notifications visible at once
# sort=-time                  # Sort newest first
# format=%s\n%b               # Summary on first line, body on second. %a for app name.
# actions=1                   # Enable actions (like buttons in notifications)

# You can define different styles for different urgencies
# [urgency=low]
# default-timeout=2000
# background-color=#A3BE8CAA
default-timeout=5000

# [urgency=critical]
# background-color=#BF616ACC
# text-color=#2E3440
# border-color=#D08770FF
# default-timeout=0 # Critical notifications stay until dismissed